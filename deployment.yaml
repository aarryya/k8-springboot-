apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8sapp-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: k8sapp
  template:
    metadata:
      labels:
        app: k8sapp
    spec:
      containers:
      - name: k8sapp
        image: aarya/k8sapp:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8080